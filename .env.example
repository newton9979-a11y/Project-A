# Copy to .env if you want to override docker-compose defaults locally.
# docker-compose.yml currently hardcodes these for simplicity — feel free
# to wire them up with `env_file: .env` once you outgrow that.

POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
RABBITMQ_DEFAULT_USER=guest
RABBITMQ_DEFAULT_PASS=guest
